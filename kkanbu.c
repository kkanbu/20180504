#include <stdio.h> // there are many definitions of functions in <stdio.h>

int main(void) // main function doesn't have input while having int as output type.
{
	printf("깐부\n"); // it prints out "Kkanbu"
	return 0; // it returns 0 to caller
}
